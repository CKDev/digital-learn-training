<section class="clearfix">
  <h2>How to Use These Resources</h2>

  <% @courses.each do |course| %>
    <%= render partial: "shared/content_widget", locals: { destination: course_path(course), title: course.title, description: course.summary } %>
  <% end %>
</section>