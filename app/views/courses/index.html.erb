<%= content_for(:callout) { %><h1>Available Training</h1><% } %>

<h2 class="screen-reader-text">All Available Training</h2>

<% if @courses.present? %>
  <%= render "listing" %>
<% else %>
  <p>There are currently no available courses.</p>
<% end %>
