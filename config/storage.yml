local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

s3:
  service: S3
  region: us-west-2
  bucket: <%= "dl-training-uploads-#{Rails.env}" %>
